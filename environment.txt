# Software environment used to produce the analyses in this repository
# (recorded 2026-06-10 on Linux x86_64, R 4.2.2)
#
# This file is informational. To reproduce, install the listed R version
# and packages — the analyses themselves are deterministic (set.seed = 20260609).

R version: 4.2.2 Patched (2022-11-10 r83330) -- "Innocent and Trusting"
Platform : x86_64-pc-linux-gnu (64-bit)
OS       : Ubuntu 22.04 LTS (or any Linux/macOS with R ≥ 4.2)

R packages (versions used):
  dplyr      1.1.4
  tidyr      1.3.1
  readr      2.x
  ggplot2    3.5.2
  patchwork  1.3.0
  scales     1.3.0
  showtext   0.9.5
  Cairo      1.6.0

System fonts:
  Helvetica (or any Helvetica-equivalent: Nimbus Sans, Arial, TeX Gyre Heros)

Disk space required for input data: ~80 MB
RAM required for largest step (MC3 matrix load): ~1 GB

Total runtime end-to-end: ~1 minute on a modern laptop.

Notes on portability:
- Scripts use `Sys.setenv(VROOM_CONNECTION_SIZE = 5e6)` to handle the wide
  MC3 matrix (~9k columns); newer readr versions auto-handle this.
- ggsave uses `cairo_pdf`; if Cairo is unavailable, replace with `device = "pdf"`.
- `showtext_auto()` registers Helvetica for vector-correct font embedding;
  if Helvetica is missing, the script falls back to system sans-serif and
  panel labels may shift very slightly.
